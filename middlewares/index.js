const ctrlWrapper = require("./controllerWrapper.js");
const getCurrent = require("./auth.js");
const validator = require("./validator");
const upload = require("./upload");

module.exports = { ctrlWrapper, getCurrent, validator, upload };
